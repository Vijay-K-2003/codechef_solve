#include <bits/stdc++.h>
#define lli long long int
using namespace std;

void solve() {
    lli a, s;
    cin >> a >> s;
    vector<int> b;
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) { 
        solve();
    }

    return 0;
}