#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    int n, q;
    cin >> n >> q;
    vector<int> a(n, 0);
    for (int i = 0 ; i < n ; i++)
        cin >> a[i];
    
    for (int i = 0 ; i < q ; i++)
    {
        int l, r, k;
        cin >> l >> r >> k;
        
    }

    return 0;
}