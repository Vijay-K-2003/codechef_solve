#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;
    vector<int> a;
    vector<pair<int, int> > v;
    for(int i = 0 ; i < n ; i++)
    {
        int temp; cin >> temp;
        a.push_back(temp);
    }

    return 0;
}