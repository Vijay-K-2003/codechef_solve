#include <bits/stdc++.h>using namespace std;int main(){	ios_base::sync_with_stdio(false);	cin.tie(NULL);		int t;	cin >> t;	while(t-- > 0)	{		string s;		cin >> s;		set<char> s1;		vector<char,int> v;		for(int i = 0 ; i < s.size() ; i++)		{			s1.insert(s[i]);		}				for(int i = 0 ;i < s.size() ;i++)		{					}				if(s.size() == 1)			cout << "0\n";		else if(s1.size() == 1)		{			cout << "1\n";		}		//	else if(s1.size()%2 != 0)	//	{	//		//int ans = floor((double)s1.size()/2 + 1);	//		//cout << ans << "\n";	//		if(s.size() == s1.size())	//		cout << floor((double) s1.size()/2) << "\n";	//		else if(s1.size() == s.size()/2)	//		cout << s1.size() << "\n";	//		else	//		{				//			//2 repetitions	//			int re = s.size() - s1.size();	//			int ans1 = re + s.size()/5;	//			int ans2 = floor((double)s1.size()/2 + 1);	//			if(re > s1.size()/2)	//		}	//	}	//	else	//	{	//		cout << s1.size()/2 << "\n";	//	}	}	return 0;}